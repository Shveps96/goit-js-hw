'use script';

const name = 'Генератор защитного поля';
let firstPrice = '1000';

const firstMessage = `Выбран ${name}, цена за штуку ${firstPrice} кредитов`;

console.log(firstMessage);

let secondPrice = '2000';

const secondMessage = `Выбран ${name}, цена за штуку ${secondPrice} кредитов`;

console.log(secondMessage);
